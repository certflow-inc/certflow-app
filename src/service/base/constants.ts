import { API_COMMON_ERRORS } from './types';

export const API_COMMON_RESPONSE_ERROR: Record<API_COMMON_ERRORS, string> = {
  API_SERVER_ERROR:
    'An error occurred while processing your request. Please, try again later'
};
